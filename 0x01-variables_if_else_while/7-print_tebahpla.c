#include <stdio.h>
/**
 * main - putchar alphabeg
 *
 * Return: Always 0
 */
int main(void)
{

	char letter;

	letter = 'z';
	while
		(lette >= 'a') {
			putchar(letter);
			c--;
		}
	putchar('\n');
	return (0);
}
